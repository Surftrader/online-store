# Завдання 1
Створіть Django-застосунок, інтернет-магазин.  

**Вимоги:**
- Створити сторінку, де виводитимуться всі товари.
- Створити сторінку, де виводитиметься інформація про один товар.
- Додати можливість авторизованому користувачеві залишати відгук/коментар про товар.
- Сторінка реєстрація/авторизація.
- Фільтрування за категоріями товарів.
- Додати можливість пошуку товару на сторінці.
- Створити сторінку для оформлення замовлення для авторизованих користувачів (без платіжної системи).

**Додаткові вимоги:**
- Можливість підписки на новини (celery).
- Команда для завантаження із зовнішньої БД (команда для manage.py).
- Переглянуті товари.  

Стилізація – на Ваш розсуд.  

**Важливі моменти:**
- Блокування переходу за меню edit/delete/create, якщо користувач не авторизований.
- Використання SQL.  

<hr>

# Django Store Professional 🛒

Повноцінний інтернет-магазин на Django з використанням асинхронних завдань, черг повідомлень та інтеграцією зовнішніх даних.

## 🚀 Стек технологій
* **Backend:** Python 3.13.7, Django 6.0
* **Database:** PostgreSQL (основна БД та ETL з зовнішніх джерел)
* **Task Queue:** Celery + Redis
* **Monitoring:** Flower
* **Frontend:** Bootstrap 5
* **Tools:** python-slugify, SQLAlchemy (для legacy баз)

## 🛠 Ключові можливості
- **Каталог товарів:** Фільтрація, детальний перегляд та історія переглядів для авторизованих користувачів.
- **Система відгуків:** Можливість залишати коментарі до товарів (з захистом від спаму через редиректи).
- **Кошик та замовлення:** Повний цикл оформлення замовлення.
- **Асинхронна підписка:** Розсилка новин через Celery + Redis (не блокує інтерфейс користувача).
- **Адмін-команди (ETL):**  
    - `import_data`: Імпорт товарів з іншої бази даних PostgreSQL.
    - `import_json`: Масове завантаження товарів з JSON-файлів.
- **Безпека:** Декоратори доступу, захист від анонімних запитів на критичні дії.

## 📦 Як запустити проект

### 1. Клонування та налаштування оточення
```bash
git clone git@github.com:Surftrader/online-store.git
cd store_project
python -m venv venv
source venv/bin/activate  # або venv\Scripts\activate на Windows
pip install -r requirements.txt
```  

### 2. Налаштування змінних оточення (Важливо!) 🔑  
Проект використовує змінні оточення для захисту конфіденційних даних.  
 1. Знайдіть у корені проекту файл `.env.example`
 2. Створіть на його основі новий файл з назвою `.env`  
 3. Заповніть у `.env` ваші дані (паролі від БД, секретний ключ Django, налаштування SMTP).  
 **Увага:** Ніколи не додавайте файл `.env` у Git!

### 3. Налаштування баз даних  

Переконайтеся, що PostgreSQL запущений. Вам потрібно створити дві бази даних:  
 1. store_db — основна база проекту.  
 2. legacy_warehouse_db — база для тестування скриптів імпорту.  

 ### 4. Запуск інфраструктури  

 Для повноцінної роботи потрібно запустити три окремі термінали:  

 #### Термінал 1 (Django):  
 ```bash
 python manage.py migrate
 python manage.py runserver
 ```  
 #### Термінал 2 (Celery Worker):  
 ```bash
 # Переконайтеся, що Redis запущений (redis-cli ping -> PONG)
 celery -A shop worker -l info --pool=solo
 ```  
 #### Термінал 3 (Flower Monitoring):  
 ```bash
 celery -A shop flower --port=5555
 ```  
 ### 5. Моніторинг  
 Ви можете відстежувати статус асинхронних завдань (наприклад, підписки) у реальному часі через Flower. Панель керування доступна за адресою: `http://localhost:5555`  

